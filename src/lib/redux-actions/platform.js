import {createAction} from 'redux-actions'

export const movePlatformLeft = createAction('MOVE_PLATFORM_LEFT');
export const movePlatformRight = createAction('MOVE_PLATFORM_RIGHT');
export const movePlatform = createAction('MOVE_PLATFORM');
